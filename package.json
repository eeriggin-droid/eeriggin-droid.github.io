{
  "name": "triathlon-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy",
    "aggregate": "node scripts/aggregateActivities.js"
  },
  "dependencies": {
    "csv-parse": "^6.0.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0"
  }
}
